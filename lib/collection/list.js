// Generated by CoffeeScript 1.6.2
(function() {
  var Base, DerivedList, List, Model, OrderedCollection, Varying, util, _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    __slice = [].slice,
    __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  Base = require('../core/base').Base;

  Varying = require('../core/varying').Varying;

  OrderedCollection = require('./collection').OrderedCollection;

  Model = require('../model/model').Model;

  util = require('../util/util');

  List = (function(_super) {
    __extends(List, _super);

    function List(list) {
      if (list == null) {
        list = [];
      }
      List.__super__.constructor.call(this, {});
      this.list = [];
      this.add(list);
    }

    List.prototype.add = function(elems, idx) {
      var elem, subidx, _i, _len,
        _this = this;

      if (idx == null) {
        idx = this.list.length;
      }
      if (!util.isArray(elems)) {
        elems = [elems];
      }
      elems = this._processElements(elems);
      if (idx === this.list.length && elems.length === 1) {
        this.list.push(elems[0]);
      } else {
        Array.prototype.splice.apply(this.list, [idx, 0].concat(elems));
      }
      for (subidx = _i = 0, _len = elems.length; _i < _len; subidx = ++_i) {
        elem = elems[subidx];
        this.emit('added', elem, idx + subidx);
        if (elem != null) {
          if (typeof elem.emit === "function") {
            elem.emit('addedTo', this, idx + subidx);
          }
        }
        if ((elem != null ? elem.isBase : void 0) === true) {
          (function(elem) {
            return _this.listenTo(elem, 'destroying', function() {
              return _this.remove(elem);
            });
          })(elem);
        }
      }
      return elems;
    };

    List.prototype.remove = function(which) {
      var idx;

      idx = this.list.indexOf(which);
      if (!(idx >= 0)) {
        return void 0;
      }
      return this.removeAt(idx);
    };

    List.prototype.removeAt = function(idx) {
      var removed;

      if (idx < 0) {
        idx = this.list.length + idx;
      }
      if (idx < 0 || idx >= this.list.length) {
        return;
      }
      removed = idx === 0 ? this.list.shift() : idx === this.list.length - 1 ? this.list.pop() : this.list.splice(idx, 1)[0];
      this.emit('removed', removed, idx);
      if (removed != null) {
        if (typeof removed.emit === "function") {
          removed.emit('removedFrom', this, idx);
        }
      }
      return removed;
    };

    List.prototype.move = function(elem, idx) {
      var oldIdx;

      oldIdx = this.list.indexOf(elem);
      if (!(oldIdx >= 0)) {
        return;
      }
      return this.moveAt(oldIdx, idx);
    };

    List.prototype.moveAt = function(oldIdx, idx) {
      var elem;

      elem = this.list[oldIdx];
      this.list.splice(oldIdx, 1);
      this.list.splice(idx, 0, elem);
      this.emit('moved', elem, idx, oldIdx);
      if (elem != null) {
        if (typeof elem.emit === "function") {
          elem.emit('movedIn', this, idx, oldIdx);
        }
      }
      return elem;
    };

    List.prototype.removeAll = function() {
      var elem, _results;

      _results = [];
      while (this.list.length > 0) {
        elem = this.list.shift();
        this.emit('removed', elem, 0);
        if (elem != null) {
          if (typeof elem.emit === "function") {
            elem.emit('removedFrom', this, 0);
          }
        }
        _results.push(elem);
      }
      return _results;
    };

    List.prototype.at = function(idx) {
      if (idx >= 0) {
        return this.list[idx];
      } else {
        return this.list[this.list.length + idx];
      }
    };

    List.prototype.watchAt = function(idx) {
      var result,
        _this = this;

      if ((idx != null ? idx.isVarying : void 0) === true) {
        return idx.flatMap(function(tidx) {
          return _this.watchAt(tidx);
        });
      }
      result = new Varying(this.at(idx));
      this.on('added', function(elem, midx) {
        if (idx === midx) {
          return result.set(elem);
        } else if ((idx > 0) && (midx < idx)) {
          return result.set(_this.at(idx));
        } else if ((idx < 0) && (midx >= (_this.list.length + idx))) {
          return result.set(_this.at(idx));
        }
      });
      this.on('removed', function(_, midx) {
        if ((idx >= 0) && (midx <= idx)) {
          return result.set(this.at(idx));
        } else if ((idx < 0) && (midx >= (this.list.length + idx))) {
          return result.set(this.at(idx));
        }
      });
      return result;
    };

    Object.defineProperty(List.prototype, 'length', {
      get: function() {
        return this.list.length;
      }
    });

    List.prototype.watchLength = function() {
      var result;

      result = new Varying(this.list.length);
      this.on('added', function() {
        return result.set(this.list.length);
      });
      this.on('removed', function() {
        return result.set(this.list.length);
      });
      return result;
    };

    List.prototype.put = function(list, idx) {
      var elem, removed, subidx, _i, _j, _len, _len1, _ref;

      if (!util.isArray(list)) {
        list = [list];
      }
      if (this.list[idx] == null) {
        this.list[idx] = null;
        delete this.list[idx];
      }
      list = this._processElements(list);
      removed = (_ref = this.list).splice.apply(_ref, [idx, list.length].concat(__slice.call(list)));
      for (subidx = _i = 0, _len = removed.length; _i < _len; subidx = ++_i) {
        elem = removed[subidx];
        this.emit('removed', elem, idx + subidx);
        if (elem != null) {
          if (typeof elem.emit === "function") {
            elem.emit('removedFrom', this, idx + subidx);
          }
        }
      }
      for (subidx = _j = 0, _len1 = list.length; _j < _len1; subidx = ++_j) {
        elem = list[subidx];
        this.emit('added', elem, idx + subidx);
        if (elem != null) {
          if (typeof elem.emit === "function") {
            elem.emit('addedTo', this, idx + subidx);
          }
        }
      }
      return removed;
    };

    List.prototype.putAll = function(list) {
      var elem, i, oldIdx, _i, _j, _len, _len1, _ref;

      _ref = this.list.slice();
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        elem = _ref[_i];
        if (!(list.indexOf(elem) >= 0)) {
          this.remove(elem);
        }
      }
      for (i = _j = 0, _len1 = list.length; _j < _len1; i = ++_j) {
        elem = list[i];
        if (this.list[i] === elem) {
          continue;
        }
        oldIdx = this.list.indexOf(elem);
        if (oldIdx >= 0) {
          this.move(elem, i);
        } else {
          this.add(this._processElements([elem])[0], i);
        }
      }
      return list;
    };

    List.prototype.shadow = function() {
      var item, newArray;

      newArray = (function() {
        var _i, _len, _ref, _results;

        _ref = this.list;
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          item = _ref[_i];
          if ((item != null ? item.isModel : void 0) === true) {
            _results.push(item.shadow());
          } else {
            _results.push(item);
          }
        }
        return _results;
      }).call(this);
      return new this.constructor(newArray, {
        parent: this
      });
    };

    List.prototype.modified = function(deep) {
      var i, isDeep, parentValue, value, _i, _len, _ref;

      if (this._parent == null) {
        return false;
      }
      if (this._parent.list.length !== this.list.length) {
        return true;
      }
      isDeep = deep == null ? true : util.isFunction(deep) ? deep(this) : deep === true;
      _ref = this.list;
      for (i = _i = 0, _len = _ref.length; _i < _len; i = ++_i) {
        value = _ref[i];
        parentValue = this._parent.list[i];
        if (value instanceof Model) {
          if (__indexOf.call(value.originals(), parentValue) < 0) {
            return true;
          }
          if (isDeep === true && value.modified(deep)) {
            return true;
          }
        } else {
          if (parentValue !== value && !((parentValue == null) && (value == null))) {
            return true;
          }
        }
      }
      return false;
    };

    List.prototype.watchModified = function(deep) {
      var isDeep, _ref, _ref1,
        _this = this;

      if (this._parent == null) {
        return new Varying(false);
      }
      isDeep = deep == null ? true : util.isFunction(deep) ? deep(this) : deep === true;
      if (isDeep === true) {
        return (_ref = this._watchModifiedDeep$) != null ? _ref : this._watchModifiedDeep$ = (function() {
          var model, react, result, uniqSubmodels, watchModel, watching, _i, _len, _ref1;

          result = new Varying(_this.modified(deep));
          react = function() {
            return result.set(_this.modified(deep));
          };
          _this.on('added', react);
          _this.on('removed', react);
          _this.on('moved', react);
          watching = {};
          watchModel = function(model) {
            return watching[model._id] = model.watchModified(deep).react(function(isChanged) {
              if (isChanged === true) {
                return result.set(true);
              } else {
                return react();
              }
            });
          };
          uniqSubmodels = _this.filter(function(elem) {
            return elem instanceof Model;
          }).uniq();
          _ref1 = uniqSubmodels.list;
          for (_i = 0, _len = _ref1.length; _i < _len; _i++) {
            model = _ref1[_i];
            watchModel(model);
          }
          uniqSubmodels.on('added', function(newModel) {
            return watchModel(newModel);
          });
          uniqSubmodels.on('removed', function(oldModel) {
            var _ref2;

            return (_ref2 = watching[oldModel._id]) != null ? typeof _ref2.stop === "function" ? _ref2.stop() : void 0 : void 0;
          });
          return result;
        })();
      } else {
        return (_ref1 = this._watchModified$) != null ? _ref1 : this._watchModified$ = (function() {
          var react, result;

          result = new Varying(_this.modified(deep));
          react = function() {
            if (_this.list.length !== _this._parent.list.length) {
              return result.set(true);
            } else {
              return result.set(_this.modified(deep));
            }
          };
          _this.on('added', react);
          _this.on('removed', react);
          return result;
        })();
      }
    };

    List.prototype._processElements = function(elems) {
      var elem, _i, _len, _results;

      _results = [];
      for (_i = 0, _len = elems.length; _i < _len; _i++) {
        elem = elems[_i];
        if (this._parent != null) {
          if ((elem != null ? elem.isModel : void 0) === true) {
            _results.push(elem.shadow());
          } else {
            _results.push(elem);
          }
        } else {
          _results.push(elem);
        }
      }
      return _results;
    };

    List.deserialize = function(data) {
      var datum, items;

      items = (function() {
        var _i, _len, _results;

        if ((this.modelClass != null) && (this.modelClass.prototype.isModel === true || this.modelClass.prototype.isCollection === true)) {
          _results = [];
          for (_i = 0, _len = data.length; _i < _len; _i++) {
            datum = data[_i];
            _results.push(this.modelClass.deserialize(datum));
          }
          return _results;
        } else {
          return data.slice();
        }
      }).call(this);
      return new this(items);
    };

    List.serialize = function(list) {
      var child, _i, _len, _ref, _results;

      _ref = list.list;
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        child = _ref[_i];
        if (child.serialize != null) {
          _results.push(child.serialize());
        } else {
          _results.push(child);
        }
      }
      return _results;
    };

    return List;

  })(OrderedCollection);

  DerivedList = (function(_super) {
    var method, _i, _len, _ref1;

    __extends(DerivedList, _super);

    function DerivedList() {
      _ref = DerivedList.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    _ref1 = ['add', 'remove', 'removeAt', 'removeAll', 'put', 'putAll', 'move', 'moveAt'];
    for (_i = 0, _len = _ref1.length; _i < _len; _i++) {
      method = _ref1[_i];
      DerivedList.prototype["_" + method] = DerivedList.__super__[method];
      DerivedList.prototype[method] = (function() {});
    }

    DerivedList.prototype.shadow = function() {
      return this;
    };

    return DerivedList;

  })(List);

  module.exports = {
    List: List,
    DerivedList: DerivedList
  };

}).call(this);
