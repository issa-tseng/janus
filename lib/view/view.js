// Generated by CoffeeScript 1.12.2
(function() {
  var Base, Varying, View, app, attribute, dynamic, isFunction, isString, match, ref, ref1, resolve, self, varying, watch,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  Base = require('../core/base').Base;

  Varying = require('../core/varying').Varying;

  ref = require('../core/types').from, dynamic = ref.dynamic, watch = ref.watch, resolve = ref.resolve, attribute = ref.attribute, varying = ref.varying, app = ref.app, self = ref.self;

  match = require('../core/case').match;

  ref1 = require('../util/util'), isFunction = ref1.isFunction, isString = ref1.isString;

  View = (function(superClass) {
    extend(View, superClass);

    function View(subject, options) {
      var vm;
      this.options = options != null ? options : {};
      View.__super__.constructor.call(this);
      this.subject = this.constructor.viewModelClass != null ? (vm = new this.constructor.viewModelClass({
        view: this,
        options: this.options,
        subject: subject
      }, {
        app: this.options.app
      }), vm.destroyWith(this), vm) : subject;
      if (typeof this._initialize === "function") {
        this._initialize();
      }
    }

    View.prototype.artifact = function() {
      return this._artifact != null ? this._artifact : this._artifact = this._render();
    };

    View.prototype._render = function() {};

    View.prototype.pointer = function() {
      return this.pointer$ != null ? this.pointer$ : this.pointer$ = match(dynamic((function(_this) {
        return function(x) {
          if (isFunction(x)) {
            return Varying.of(x(_this.subject));
          } else if (isString(x) && (_this.subject.watch != null)) {
            return _this.subject.watch(x);
          } else {
            return Varying.of(x);
          }
        };
      })(this)), watch((function(_this) {
        return function(x) {
          return _this.subject.watch(x);
        };
      })(this)), attribute((function(_this) {
        return function(x) {
          return new Varying(_this.subject.attribute(x));
        };
      })(this)), varying((function(_this) {
        return function(x) {
          if (isFunction(x)) {
            return Varying.of(x(_this.subject));
          } else {
            return Varying.of(x);
          }
        };
      })(this)), app((function(_this) {
        return function(x) {
          if (x != null) {
            return _this.options.app.watch(x);
          } else {
            return new Varying(_this.options.app);
          }
        };
      })(this)), self((function(_this) {
        return function(x) {
          if (isFunction(x)) {
            return Varying.of(x(_this));
          } else {
            return Varying.of(_this);
          }
        };
      })(this)));
    };

    View.prototype.wireEvents = function() {
      if (!this._wired) {
        this._wireEvents();
      }
      this._wired = true;
    };

    View.prototype._wireEvents = function() {};

    return View;

  })(Base);

  module.exports = {
    View: View
  };

}).call(this);
